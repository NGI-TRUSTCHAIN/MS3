export class DBRepository {

    constructor(args:any) {
        // Initialize the DB connection.
        // set a connection pool.
    }

    set(args:any): any  {
      // Set data in DB. 
    }

    get(args:any): any {
        // Get data from DB.
    }
  }