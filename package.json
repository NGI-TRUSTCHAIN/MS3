{
  "name": "m3s",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build:utils && npm run build:wallet",
    "build:utils": "cd packages/utils && tsc",
    "build:wallet": "cd packages/wallet && tsc",
    "publish:utils": "cd packages/utils && node publish.js",
    "publish:wallet": "npm run publish:utils && cd packages/wallet && node publish.js"
  },
  "devDependencies": {
    "typescript": "^4.9.5"
  }
}