document.getElementById('swap').addEventListener('click', async () => {
  document.getElementById('status').textContent = 'Crosschain swap not yet implemented.';

  // The participant is expected to:
  // - Import and initialize the crosschain module
  // - Connect a wallet with tokens on a source network
  // - Configure the destination network and token
  // - Call the swap function
  // - Print the transaction hash in the console and update the status on screen
});
